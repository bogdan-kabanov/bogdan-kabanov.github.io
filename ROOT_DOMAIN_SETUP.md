# Настройка для корневого домена GitHub Pages

## ✅ Что уже сделано:
1. `index.html` использует абсолютные пути от корня (`/logo.png`, `/static/js/...`)
2. Создан файл `.nojekyll` для правильной работы React приложения
3. Все пути настроены для корневого домена

## 🔧 Важно: Название репозитория

Для того чтобы сайт работал по адресу **https://bogdan-kabanov.github.io/** (без подпапки), репозиторий должен называться:
**`bogdan-kabanov.github.io`** (не `oaa`)

### Вариант 1: Переименовать текущий репозиторий
1. Перейдите в Settings репозитория на GitHub
2. Прокрутите вниз до "Repository name"
3. Измените название с `oaa` на `bogdan-kabanov.github.io`
4. Нажмите "Rename"

### Вариант 2: Создать новый репозиторий
1. Создайте новый репозиторий с именем `bogdan-kabanov.github.io`
2. Измените remote в локальном репозитории:
   ```bash
   git remote set-url origin https://github.com/bogdan-kabanov/bogdan-kabanov.github.io.git
   git push -u origin main
   ```

## 📝 Выполните пуш:

```bash
bash quick_push.sh
```

Или вручную:
```bash
git add .
git commit -m "Configure for root domain GitHub Pages"
git push origin main
```

## 🌐 После настройки:

Сайт будет доступен по адресу:
**https://bogdan-kabanov.github.io/**

Все ресурсы (изображения, CSS, JS) будут загружаться с корневого домена:
- `/images/Main/Background.png`
- `/static/js/main.bd9139ee.js`
- `/static/css/main.ea096d23.css`

